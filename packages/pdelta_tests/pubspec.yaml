name: pdelta_tests
description: Tests for pdelta.
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  protobuf: ^1.0.1
  fixnum: ^0.10.11
  ptypes:
    path: ../ptypes
  pdelta_tests_clothes:
    path: ../pdelta_tests_clothes
  pdelta:
    path: ../pdelta

dev_dependencies:
  build_runner: any
  test: ^1.14.3
