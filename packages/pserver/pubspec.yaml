name: pserver
description: Server helper functions for implementations using Google Firestore as the persistence database.
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  protobuf: ^2.1.0
  fixnum: ^1.0.0
  hive: ^1.4.1
  pdelta:
    path: ../pdelta